# Nix OS setup

This repo contains my config files as well as script to manage their versioning.

To use it on new machine it might be necessary to change ownership of the /etc/nixos directory with `sudo chown -R youruser:users /etc/nixos` and change the script to be executable with `chmod +x /etc/nixos/rebuild.sh`.

